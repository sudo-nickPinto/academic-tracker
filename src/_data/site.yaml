title: "My Academic Dashboard"
current_term: ""
last_refreshed: ""
# "owner/repo" on GitHub — used by the on-site Manage/Settings pages (src/js/gh-client.js)
# to know which repo to commit to via the GitHub REST API. Not a secret.
# Blank until you run /setup/ (or set it by hand) — see CLAUDE.md's "refresh" workflow.
repo: ""
# Workflow file (in .github/workflows/) that rebuilds and pushes docs/ — dispatched
# automatically after an on-site edit so the live site picks it up.
rebuild_workflow: "nightly-rebuild.yml"
