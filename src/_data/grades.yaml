# Grade entries per class. `category` should match a name in that class's
# `grade_categories` (see classes.yaml) so it counts toward the weighted average —
# anything else lands in an "Other" bucket that's shown but not weighted in.
# - id: <slug>
#   class_id: <class id>
#   item: "Test 1"
#   category: "Exams"
#   score: 87
#   max: 100
#   date: "YYYY-MM-DD" # optional
[]
