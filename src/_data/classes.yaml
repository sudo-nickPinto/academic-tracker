# One entry per class. `id` is a slug used in URLs — keep it lowercase, no spaces.
# Classes with unknown details (no syllabus added yet) use "TBD" placeholders — replace when known.
# grade_categories: optional list of { name, weight } (weight = percent of final grade, e.g. 20).
# Grade entries in grades.yaml reference a category by name; leave [] until you know the real breakdown.

[]
